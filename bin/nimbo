#!/usr/bin/env ruby

require 'nimbo'

suite_dir = ARGV[0] || '.'

nimbo_client = Nimbo::Client.new("http:://api.nimbolab.com")
archive_archive = nimbo_client.archive(suite_dir)

puts nimbo_client.request(suite_archive)