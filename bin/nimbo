#!/usr/bin/env ruby

require 'nimbo'

suite_dir = ARGV[0] || '.'

nimbo_client = Nimbo::Client.new 'localhost:4000', ENV['TMPDIR']
suite_archive = nimbo_client.archive(suite_dir)

puts nimbo_client.request(suite_archive)